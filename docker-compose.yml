services:
  meilisearch:
    image: getmeili/meilisearch:v1.12
    container_name: poc-meilisearch
    ports:
      - "7700:7700"
    environment:
      MEILI_MASTER_KEY: masterKey
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - ./meili_data:/meili_data
